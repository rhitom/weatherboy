[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build --workspace @weatherboy/worker"]

[start]
cmd = "npm run start --workspace @weatherboy/worker"
